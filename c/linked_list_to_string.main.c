#include "linked_list_to_string.c"
#include <stdio.h>

int main()
{
    printf("%s\n", stringify(&((Node){ .data = 1, .next = &((Node){ .data = 2, .next = &((Node){ .data = 3, .next = NULL }}))}; return 0; }
